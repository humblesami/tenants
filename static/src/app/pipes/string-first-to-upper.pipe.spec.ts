import { StringFirstToUpperPipe } from './string-first-to-upper.pipe';

describe('StringFirstToUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new StringFirstToUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
